pre-commit:
  commands:
    check:
      run: mise run check
      skip:
        - merge
        - rebase
    check-links:
      run: mise run check-links
      skip:
        - merge
        - rebase
